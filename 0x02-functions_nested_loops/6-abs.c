#include "main.h"
/**
 * _abs - gets absolute value
 * Return: num
 * @n: passed num
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
	return (0);
	}
}
